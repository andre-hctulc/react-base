// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./Stat.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "Stat.tsx", path: "components/data-display/Stat.tsx", demos: demos || [] };

export default mod;
