// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./LiveChange.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "LiveChange.tsx", path: "components/inputs/LiveChange.tsx", demos: demos || [] };

export default mod;
