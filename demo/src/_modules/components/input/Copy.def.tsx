// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./Copy.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "Copy.tsx", path: "components/input/Copy.tsx", demos: demos || [] };

export default mod;
