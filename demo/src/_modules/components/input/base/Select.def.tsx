// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./Select.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "Select.tsx", path: "components/input/base/Select.tsx", demos: demos || [] };

export default mod;
