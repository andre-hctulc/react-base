// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./ConfirmDialog.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "ConfirmDialog.tsx", path: "components/dialogs/dialog/ConfirmDialog.tsx", demos: demos || [] };

export default mod;
