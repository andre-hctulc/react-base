// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./DialogHeader.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "DialogHeader.tsx", path: "components/dialogs/dialog/DialogHeader.tsx", demos: demos || [] };

export default mod;
