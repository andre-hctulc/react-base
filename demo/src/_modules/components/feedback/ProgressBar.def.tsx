// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./ProgressBar.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "ProgressBar.tsx", path: "components/feedback/ProgressBar.tsx", demos: demos || [] };

export default mod;
