// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./Alert.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "Alert.tsx", path: "components/feedback/alerts/Alert.tsx", demos: demos || [] };

export default mod;
