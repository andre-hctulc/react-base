// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./Skeleton.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "Skeleton.tsx", path: "components/feedback/Skeleton.tsx", demos: demos || [] };

export default mod;
