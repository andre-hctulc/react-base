// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./Spinner.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "Spinner.tsx", path: "components/feedback/Spinner.tsx", demos: demos || [] };

export default mod;
