// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./GlobalHeader.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "GlobalHeader.tsx", path: "components/layout/GlobalHeader.tsx", demos: demos || [] };

export default mod;
