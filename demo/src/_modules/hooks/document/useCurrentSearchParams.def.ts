// auto generated by script 'demo-prepare'

import type { ModuleDef, DemoDef } from "src/types";

const demos: DemoDef[] = Object.values(import.meta.glob("./useCurrentSearchParams.demo*.tsx", { import: "default", eager: true }));

const mod: ModuleDef = { name: "useCurrentSearchParams.ts", path: "hooks/document/useCurrentSearchParams.ts", demos: demos || [] };

export default mod;
