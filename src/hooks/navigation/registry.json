{
    "$schema": "https://ui.shadcn.com/schema/registry.json",
    "items": [
        {
            "name": "use-hash",
            "type": "registry:hook",
            "title": "useHash",
            "description": "Reads and writes the URL hash.",
            "files": [{ "path": "use-hash.ts", "type": "registry:hook" }]
        }
    ]
}
